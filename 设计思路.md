# 设计思路 — 多变量时间序列预测 (v2)

## 任务
基于 96 步历史，预测未来 {96, 192, 336, 720} 步、100 变量的时间序列。
评价指标：四个 horizon 的平均 MSE。

## 数据分析
- 训练数据 `train.csv` 20000×100，已 **Min-Max 归一化到 [0, 1]**（mean≈0.44, std≈0.20）
- 训练集是**单一长序列**，通过滑动窗口构造 (96 input → L output) 训练样本
- 验证集严格取最后 10%，**无 train→val 标签 leakage**

## 模型设计 — 多模型 × 多 seed 大规模 ensemble

针对每个 horizon 分别训练以下模型，所有模型内置 **RevIN 实例归一化**：

### V1 模型（每模型 1 个 seed）
| Model | 思想 | 参数量 (pl=96) |
|---|---|---|
| **DLinear** (k=15/25/51) | Moving-Avg 分解趋势/季节 + 共享 Linear | ~19K |
| **DLinear-MLP** | DLinear 升级版，GELU 隐藏层 (hidden=256) | 89K |
| **NLinear** | 减去末值的简化线性模型 | 9K |
| **iTransformer** | 倒置注意力，变量维度做 Transformer (d=128, layers=2) | 290K |
| **iTransformer-XL** | 增大版 (d=192, layers=3)，仅 pl=336/720 | 1.05M |

### V2 强化模型（每模型 3 个 seed: 2026/42/1234）
| Model | 思想 | 参数量 (pl=96) |
|---|---|---|
| **TSMixer** | 时间+通道双向 MLP 混合 (n_blocks=4, d=128) | 189K |
| **TimeMixer** | 简化 TimeMixer，多层 MLP per-channel + cross-channel | 345K |
| **PatchTST-Strong** | PatchTST 加深加宽 (d=128, layers=3) + dropout | 549K |
| **iTransformer-Strong** | iTransformer 加大版 (d=256, layers=3) + affine RevIN + dropout | 1.6M |

**总计 74 个 checkpoint**（4 horizon × ~19 模型变体）。

### Ensemble 策略
对每个 horizon **独立**做：
1. **Greedy forward selection**：在 val 集上贪心加入最能降低 MSE 的模型。
2. **Top-K by individual val MSE**（K=3,5,7,10）
3. **mean-of-all**

取上述四种策略中 val MSE 最低者作为该 horizon 的最终集成。
集成模型预测取算术平均，clip 到 [0, 1]。

## 训练细节
- **环境**：服务器 2× NVIDIA RTX A6000 (48G)，PyTorch 2.0.1 + CUDA 11.7
- **Optimizer**：AdamW (betas=(0.9, 0.99))，weight_decay=1e-4
- **Scheduler**：warmup-cosine（warmup 5%）+ min_lr_ratio=0.01
- **Loss**：MSE
- **Gradient Clipping**：1.0
- 批大小：DLinear 系列 256；iTransformer-Strong/PatchTST-Strong 128
- Epochs: V1 系列 30，V2 系列 40
- Patience: 10（early stopping）
- Seed: 2026, 42, 1234

## 验证集结果

| Model | pl=96 | pl=192 | pl=336 | pl=720 |
|---|---:|---:|---:|---:|
| DLinear k=25 | 0.00450 | 0.00474 | 0.00520 | 0.00595 |
| DLinear-MLP | 0.00440 | 0.00473 | 0.00524 | 0.00600 |
| iTransformer | 0.00444 | 0.00470 | 0.00520 | 0.00595 |
| iTransformer-XL | - | - | 0.00517 | 0.00592 |
| TSMixer (3-seed avg) | 0.00355 | 0.00399 | 0.00441 | 0.00503 |
| TimeMixer (3-seed avg) | 0.00378 | 0.00419 | 0.00444 | 0.00515 |
| PatchTST-Strong (3-seed avg) | 0.00354 | 0.00395 | 0.00444 | 0.00521 |
| **iTransformer-Strong (3-seed avg)** | **0.00323** | **0.00378** | **0.00418** | **0.00494\*** |
| **🏆 Best Ensemble (greedy)** | **0.00300** | **0.00354** | **0.00382** | **0.00416** |

\* itransformer_strong pl=720 s2026 收敛差 (0.00594)，s42/s1234 表现最好 (0.00438/0.00451)

### Test_demo 5-样本评估

| pl | MSE |
|---|---:|
| 96 | 0.002497 |
| 192 | 0.003027 |
| 336 | 0.003140 |
| 720 | 0.003458 |
| **AVG** | **0.003030** |

## 关键设计决策

1. **多模型 + 多 seed 大集成**：在 GPU 加持下训练 74 个 checkpoint，多样性显著降低集成方差。
2. **iTransformer-Strong 是关键**：变量维度注意力（100 变量间的相关性）+ 加大容量后，单模型 MSE 已显著优于所有 baseline，在 pl=720 上提升 26%。
3. **TSMixer / TimeMixer / PatchTST 提供互补性**：通道混合 MLP 与 patch-Transformer 在不同 horizon 上抓取不同模式。
4. **Greedy ensemble selection**：在 val 集上贪心选模型，自动剔除拖后腿的模型；每个 horizon 选择 3-8 个模型。
5. **RevIN 实例归一化**（带 affine）：稳定优化、对窗口分布漂移鲁棒。
6. **AdamW + warmup-cosine**：相对原版 Adam+Cosine 在大模型上更稳。
7. **Clip 到 [0, 1]**：与训练数据值域一致，去掉极小的越界预测噪声。

## 一键复现
```bash
# 训练（GPU，并行双卡 ~1.5h）
bash train_gpu0.sh   # 卡 0
bash train_gpu1.sh   # 卡 1

# 选择最优 ensemble (val 集贪心)
python eval_v2.py    # 生成 ensemble_config.json

# 推理
python predict_v2.py --test_path /path/to/test.npy

# 打包
python make_submission_v2.py --names "张三-学号,..."
```
