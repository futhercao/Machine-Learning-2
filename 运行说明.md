# 运行说明 — 多变量时间序列预测 (v2)

## 环境
- Python 3.10+
- PyTorch 2.0.1+cu117 (CUDA 11.7) — CPU 也可推理
- numpy, pandas

```bash
pip install torch numpy pandas
```

## 目录结构
```
TimeSeriesProject/
├── data_loader.py          # 滑窗数据加载（无 leakage）
├── models.py               # V1: DLinear / NLinear / iTransformer / PatchTST
├── models_v2.py            # V2: TSMixer / TimeMixer / PatchTST-Strong / iTransformer-Strong
├── train.py                # V1 单模型训练（Adam + Cosine）
├── train_v2.py             # V2 单模型训练（AdamW + warmup-cosine + seed）
├── train_gpu0.sh           # GPU 0 训练脚本
├── train_gpu1.sh           # GPU 1 训练脚本
├── predict_v2.py           # 用 ensemble_config.json 推理 ⭐ 现场用这个
├── predict_final.py        # 老版自动集成（fallback）
├── eval_v2.py              # 评估所有 ckpt 并贪心选最优 ensemble
├── eval_demo.py            # 在 test_demo/ 上评估
├── eval_val.py             # 在训练集 val 切分上评估
├── make_submission_v2.py   # 打包 zip
├── ensemble_config.json    # 每个 horizon 的最优 ensemble 配置
├── checkpoints_v2/         # 74 个训练好的 .pt（V1 + V2 × 3 seeds）
├── results_v2/             # 输出 pred_{96,192,336,720}.npy
├── 设计思路.md
└── 运行说明.md
```

## 训练（已完成）
```bash
# 卡 0
nohup bash train_gpu0.sh > logs_v2/_gpu0.log 2>&1 &
# 卡 1
nohup bash train_gpu1.sh > logs_v2/_gpu1.log 2>&1 &
```
两卡并行约 1.5 小时。

## Ensemble 选择（已完成）
```bash
python eval_v2.py
# 输出每个 horizon 各模型 val MSE，贪心选择最优组合
# 写入 ensemble_config.json
```

## 现场验收推理（核心）
拿到测试集 `test.npy`（shape (250, 96, 100)）后：
```bash
python predict_v2.py --test_path /path/to/test.npy --device cuda
# 或 CPU:
python predict_v2.py --test_path /path/to/test.npy --device cpu
```
输出在 `results_v2/`：
- `pred_96.npy`  (250, 96, 100)
- `pred_192.npy` (250, 192, 100)
- `pred_336.npy` (250, 336, 100)
- `pred_720.npy` (250, 720, 100)

dtype float32，clip 到 [0, 1]，**GPU 推理总耗时 < 1 分钟**。

## 打包提交
```bash
python make_submission_v2.py --names "张三-2021001,李四-2021002,王五-2021003"
# 生成 时间序列-张三-2021001-李四-2021002-王五-2021003.zip
```

## 验证 demo 数据
```bash
python eval_demo.py --pred_dir results_v2
# 期望输出: Average MSE ≈ 0.003
```

## 备注
- `predict_v2.py` 读取 `ensemble_config.json` 决定每 horizon 用哪些模型集成
- 训练用 seed 2026, 42, 1234，固定可复现
- 验证集严格 split，无 train→val 标签 leakage
